#machine:
  #environment:
    #ADB_INSTALL_TIMEOUT: 80
    #GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx2048m -XX:+HeapDumpOnOutOfMemoryError"'

general:
  #branches:
    #only:
    #  - master # list of branches to build

  artifacts:
      - ./app/build/outputs/apk/
     # - ./app/build/outputs/reports/androidTests/connected/

dependencies:
   pre:
      - echo y | android update sdk --no-ui --all --filter "tools"
      - echo y | android update sdk --no-ui --all --filter "build-tools-24.0.0"
      - echo y | android update sdk --no-ui --all --filter "extra-google-m2repository"

test:
   override:
      #- ./gradlew clean test assembleRelease -PdisablePreDex -i -s -Pcom.android.build.threadPoolSize=1 -Dorg.gradle.parallel=false -Dorg.gradle.jvmargs="-Xms512m -Xmx512m" -Dorg.gradle.daemon=false
       - (./gradlew clean assembleRelease):
         timeout: 360

#deployment:
   #production:
      #branch: master
      #commands:
       #  - ./gradlew crashlyticsUploadDistributionDevRelease
       #  - ./gradlew crashlyticsUploadDistributionProdRelease
       #  - scripts/publish_artifacts.sh
