
dependencies:
   pre:
      - echo y | android update sdk --no-ui --all --filter 
      "tools",platform-tools,build-tools-24.0.0,android-4,extra-google-m2repository,
      extra-google-google_play_services,extra-android-support

test:
   override:
       - ./gradlew clean assembleRelease -PdisablePreDex -i -s -Pcom.android.build.threadPoolSize=1 -Dorg.gradle.parallel=false -Dorg.gradle.jvmargs="-Xms512m -Xmx512m" -Dorg.gradle.daemon=false
